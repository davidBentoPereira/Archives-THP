<nav id="topNav" class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="navbar-collapse left collapse">
    <ul class="nav navbar-nav">
      <li class="nav-item">
        <a class="nav-link active gradient-text" href="<%=root_path %>">Accueil</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active gradient-text" href="<%=new_event_path  %>">Ajout évènement</a>
      </li>
    </ul>
  </div>
  <!-- Logo -->
  <a class="navbar-brand mx-auto" href="<%=root_path %>">
    <img src="<%= image_path "logo.png" %>" width="50" alt="logo"> Make It Happen
  </a>
  <div class="navbar-collapse right collapse">
    <ul class="nav navbar-nav  w-100 justify-content-end">
    </ul>
  </div>
  <% if user_signed_in? %>
    <a class="btn btn-outline-light btn-sm ml-auto btn-2 mr-3" href="<%= user_path(current_user) %>"><i class="fas fa-user"></i> Profil </a>
    <a class="btn btn-outline-light btn-sm ml-auto btn-2" href="<%= destroy_user_session_path %>" data-method="delete"
       data-confirm="Un bébé licorne meurt chaque fois qu'un utilisateur se déconnecte. Voulez-vous vraiment vous déconnecter ?">
      <i class="fas fa-sign-in-alt"></i> Déconnexion
    </a>

    <!-- Humberger Button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

  <% else %>
    <a class="btn btn-outline-light btn-sm ml-auto btn-2 mr-3" href="<%=new_user_registration_path %>"><i class="fas fa-sign-in-alt"></i> S'inscrire</a>
    <a class="btn btn-outline-light btn-sm ml-auto btn-2" href="<%=new_user_session_path %>"><i class="fas fa-sign-in-alt"></i> Connexion</a>

    <!-- Humberger Button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  <% end %>
</nav>
