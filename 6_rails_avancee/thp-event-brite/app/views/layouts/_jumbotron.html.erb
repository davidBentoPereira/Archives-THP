<!-- JUMBOTRON -->
<section>
  <div class="jumbotron jumbotron-fluid">
    <div class="container text-center text-light">
      <h1 class="display-4"> Make It Happen</h1>
      <h2 class="h3">Organise des évènements chaud bouillant près de chez toi, fais-toi payer pour ça et roules-toi dans la flouze !</h2>
      <% if user_signed_in? %>

        <a type="button" class="btn btn-4 text-dark btn-lg  m-3" href="<%=new_event_path  %>">Je créé mon évènement</a>

      <% else %>
        <a type="button" class="btn btn-1 text-dark btn-lg m-3" href="<%=new_user_session_path %>">Je me connecte</a>
<!--        <a type="button" class="btn btn-4 text-dark btn-lg  m-3" href="<%#=new_user_session_path %>">Je créé mon évènement</a>-->
        <a type="button" class="btn btn-4 text-dark btn-lg  m-3" href="<%=new_event_path %>">Je créé mon évènement</a>
      <% end %>
    </div>
  </div>
</section>
